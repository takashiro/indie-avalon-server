
class UnitTest {

	/**
	 * Create a unit test
	 * @param {string} name
	 */
	constructor(name) {
		this.name = name;
	}

	/**
	 * Tests to be run
	 * @param {HttpClient} client
	 */
	run(client) {
	}

}

module.exports = UnitTest;
